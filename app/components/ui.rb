# frozen_string_literal: true

# Namespace for the inherited design-system primitives.
#
# If a SpecGuard surface needs a primitive that is not here, ADD IT to this library in yatfa's
# style — never one-off it in a view. See the "SpecGuard — Design System" spec, hard rule #1.
module UI
end

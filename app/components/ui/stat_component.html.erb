<div class="<%= wrapper_class %>">
  <p class="section-label text-app-content-secondary"><%= label %></p>
  <p class="text-app-h1 tabular-nums <%= value_class %>"><%= value %></p>
  <% if hint.present? %>
    <p class="text-xs text-app-muted"><%= hint %></p>
  <% end %>
</div>

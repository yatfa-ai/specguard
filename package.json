{
  "name": "specguard",
  "private": true,
  "description": "The CSS build. Tailwind and DaisyUI both come from npm and are pinned by package-lock.json; app/assets/builds/ is generated and never committed. cssbundling-rails runs build:css from assets:precompile, so the image build produces its own stylesheet.",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.19",
    "tailwindcss": "^4.3.3"
  },
  "scripts": {
    "build:css": "tailwindcss -i ./app/assets/tailwind/application.css -o ./app/assets/builds/application.css --minify",
    "watch:css": "tailwindcss -i ./app/assets/tailwind/application.css -o ./app/assets/builds/application.css --watch"
  }
}
